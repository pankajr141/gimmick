Installation

Installation of gimmick is straight forward, just use pip. All the dependency will be autoresolved
Its a complete python based project, hence a simple pip will resolve all the dependency.


- Install via pip::

    sudo pip install gimmick
